/*global __g_sm, ko, _, _campaignID, Tracx, Q, _campaignDateFormat, $, _campaignTimeZone */
define(
    [

    ],
    function()
    {

        'use strict';

        /**
         * @constructor
         */
        function Forms()
        {
        }

        return Forms;
    }
);